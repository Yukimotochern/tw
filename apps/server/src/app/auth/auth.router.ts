/**
 * # Password sign up
 * * email + password -> get confirm email with 6 digit code
 * * expire in 5 tries or 15 minutes
 * * confirmed account -> Welcome email and redirect to '/'
 * # Forget password
 * * email + newPassword -> get confirm email with 6 digit code
 * * expire in 5 tries or 15 minutes
 * * reset successfully and redirect to '/'
 * # Password login
 * # Oauth sign in
 * * generate and redirect to google
 * * grandted
 * * oauth callback
 *  * error redirect and display message
 *  * get email and create account
 *    * if exist redirect and login
 *    * if not exist create account and redirect
 *  * / page handle get user
 */
